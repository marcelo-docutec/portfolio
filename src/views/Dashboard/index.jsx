import React from 'react';
import Button from '@material-ui/core/Button';

const Dashboard = () => {
  return (
    <div>
      <div>Dashboard</div>
      <Button color="primary" variant="contained" href="/as">
        See projects
      </Button>
    </div>
  );
};

export default Dashboard;
